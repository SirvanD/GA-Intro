//hard code inputs

let user = 'partnered';
let relStatus = 'serious';
if (user === 'partnered' && relStatus === 'serious') {
    console.log('I know the best place for a romantic date night !')
}else  {
    console.log('No worries ! head to the closest pub')
};